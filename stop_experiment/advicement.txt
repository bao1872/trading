明确建议如下。

## 先定结论

**你现在不要再继续加功能。先把当前版本定成“可用的手动交易操作系统 v1”。**

原因很清楚：

1. **主线生产链已经能跑通**

   * 每日盘后可以产出 T+1 建议
   * 有账户净值曲线
   * 有历史交易报告
   * 有操作包。

2. **模拟盘核心完整性已经过关**

   * 参数一致性 PASS
   * 决策一致性 PASS
   * 状态一致性 PASS
   * CSV 输出基本 PASS
   * 只有 Layer 2 预测源仍是已知问题。

3. **你当前最大问题已经从“功能缺失”变成“界面和流程太乱”**
   所以现在最重要的不是继续扩逻辑，而是**收口、固定、形成日常使用流程**。

---

# 我建议你下一步只做 3 件事

## 1. 立刻冻结“日常使用口径”

以后你每天盘后只看这 3 个东西：

### A. 操作包

路径：

* `output/live/operator_packets/YYYY-MM-DD/`

你主要看：

* `02_account_snapshot.md`
* `03_t1_action_plan.md`
* `04_trade_history_summary.md`
* `05_equity_curve.png`

### B. 飞书摘要

飞书只负责提醒，不负责承载全部细节。

### C. 账本只做底层存证

这些你平时不要主动看，除非排错：

* `holdings/*.parquet`
* `live/decisions/*.parquet`
* `live/executions/*.parquet`

**这是第一条硬规则。**
否则你还是会每天陷进一堆底层文件里。

---

## 2. 把 `run_daily.py` 定成唯一入口

以后不要再手动分别跑：

* 07
* 09
* 10
* 11
* 12

**统一只跑 `run_daily.py`。**

你现在已经把 Step 7 接进去了，所以以后生产流程就固定为：

```text
run_daily.py
→ 07 predictions
→ 09 paper trading
→ 10 T+1 action plan
→ 11 feishu summary
→ 12 operator packet
```

### 你要做的唯一动作

给自己定一个固定命令，比如：

```bash
python stop_experiment/run_daily.py --date YYYY-MM-DD
```

然后把它写到 README 最前面，作为唯一生产入口。

---

## 3. 暂停所有“策略扩展”，只保留一个技术债

**现在唯一值得继续追的技术问题，就是 Layer 2 预测源不一致。**

这件事你已经查得很清楚了：

* live 模式下，`07_generate_daily_predictions.py` 从 DB 实时拉特征
* 和训练/回测时的特征源有差异
* 导致预测不同
* top10 overlap 很低。

### 这件事怎么处理

我建议你把它单独列成一个项目，不和日常盘后系统混做。

项目名就叫：

**“冻结 live 预测特征源”**

在没解决之前，明确写死一句话：

> **当前生产系统可用，但 live 预测和回测预测并非同一来源；一致性检查以 replay 为准，生产使用 operator packet。**

也就是说，**先接受这个已知问题，不要因此阻塞日常使用。**

---

# 你现在不要做的事

## 不要做 1：不要继续重构回测主引擎

现在已经够用了。
再重构只会把系统重新搞乱。

## 不要做 2：不要继续扩仓位实验

W1a 已经有阶段性答案了，但现在还没必要切到生产。
先保持生产等权。

## 不要做 3：不要再加更多报表

你现在不是缺报表，是缺“只看哪几个”的纪律。

---

# 我建议你给自己定一个固定日常流程

## 每天收盘后

只做这 4 步：

### Step 1

运行：

```bash
python stop_experiment/run_daily.py --date YYYY-MM-DD
```

### Step 2

看飞书摘要
确认：

* 账户净值
* 当日收益
* 明日买几只 / 卖几只

### Step 3

打开操作包，只看两份：

* `02_account_snapshot.md`
* `03_t1_action_plan.md`

### Step 4

手动下单
按 `03_t1_action_plan.md` 执行。

---

# 我建议你再补两个很小的动作

## 动作 A：把操作包目录做成“最新快捷入口”

例如额外生成一个：

* `output/live/operator_packets/latest/`

永远指向最新日期。
这样你不用每天找目录。

## 动作 B：把 `run_daily.py` 成功后在终端最后打印一句

类似：

```text
今日操作包：
output/live/operator_packets/2026-05-08/
重点查看：
- 02_account_snapshot.md
- 03_t1_action_plan.md
```

这会让你日常使用顺很多。

---

# 最终明确建议

**现在就停手，不再扩功能。**

接下来只做这三件事：

1. **冻结当前版本为“手动交易操作系统 v1”**
2. **以后只通过 `run_daily.py` 使用系统**
3. **把 Layer 2 预测源不一致单独列为唯一待修技术债**

一句话总结：

**你现在已经够用了，下一步不是继续开发，而是强制自己只用“飞书摘要 + 操作包 + run_daily.py”这条简化流程。**
