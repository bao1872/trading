明确建议：

## 结论

**这个问题应该按“主线生产闭环”处理，不是单独补一个报表。**
也就是把这次新增的两类产物：

* `live_equity_curve.csv`
* `live_trade_report.csv`

视为**每日生产流水线的正式输出**，而不是临时分析文件。根据你给出的执行结果，`09_paper_trading_runner.py` 已经承担了三件事：修正 `cur_ret`、生成净值曲线、生成交易报告；`11_feishu_notification.py` 也已经读取净值并在消息里展示。

---

## 这个问题该怎么处理

### 1. 不要再单独改回测逻辑

现在这个处理方向是对的：

* **不改原回测逻辑**
* 在生产侧新增：

  * `build_live_equity_curve.py`
  * `build_live_trade_report.py`
* 并由 `09_paper_trading_runner.py` 在 live 流程里统一触发

这是最稳的方案，因为：

* 不会污染研究侧基线
* 生产侧账户净值和交易报告是从真实执行账本重建的
* 飞书通知也能直接复用这些产物。

### 2. `run_daily.py` 必须同步更新

**要同步更新。答案是明确的：要。**

理由很简单：
你现在已经把“净值曲线 + 交易报告”纳入了 `09_paper_trading_runner.py` 的 live 后处理，而每日真正跑生产的是 `run_daily.py`。如果 `run_daily.py` 只是跑到预测/决策/执行就结束，或者没有经过 `09` 的 live 模式，那这两类新产物就不会每天自动更新。

所以你要把它当成生产链正式步骤，而不是“手动跑一次 09 看看”。

---

## 我给你的明确处理建议

### 方案 A：最推荐

**让 `run_daily.py` 继续调用 `09_paper_trading_runner.py --mode live`，不要在 `run_daily.py` 里重复实现净值和交易报告逻辑。**

也就是：

* `run_daily.py` 只负责调度
* `09_paper_trading_runner.py` 负责：

  * 保存 decisions
  * 重建 `live_equity_curve.csv`
  * 重建 `live_trade_report.csv`
* `11_feishu_notification.py` 负责读这些产物并发消息

这样职责最清晰，也最不容易产生双份逻辑。根据你给出的结果，`09` 和 `11` 这边已经具备这个能力了。

### 方案 B：不要做

不要在 `run_daily.py` 里再手写一遍：

* 账户净值曲线构建
* 交易配对报告构建

否则以后修一个地方，另一个地方忘了改，很快就会口径漂移。

---

## `run_daily.py` 具体要确认什么

你现在应该检查 `run_daily.py` 是否满足下面 4 条：

### 必须有

1. **调用 `07_generate_daily_predictions.py`**
2. **调用 `09_paper_trading_runner.py --mode live`**
3. **调用 `10_tomorrow_action_plan.py`**
4. **调用 `11_feishu_notification.py`**

### 顺序建议

最合理的顺序是：

```text
07_generate_daily_predictions
→ 09_paper_trading_runner --mode live
→ 10_tomorrow_action_plan
→ 11_feishu_notification
```

因为：

* `09` 会生成最新执行账本、净值曲线、交易报告
* `10` 负责 T+1 操作建议
* `11` 最后读取最新净值和建议做通知

---

## 每日生产验收标准

我建议你给 `run_daily.py` 增加一个非常明确的验收清单。
每天跑完后，至少检查以下文件是否更新成功：

### 核心账本

* `output/live/decisions/<date>.parquet`
* `output/live/executions/<date>.parquet`

### 新增产物

* `output/live/live_equity_curve.csv`
* `output/live/live_trade_report.csv`

### T+1 决策建议

* `output/live/action_plans/<date>.md`

### 通知侧

* 飞书消息里包含：

  * 净值
  * 日收益
  * 回撤
  * T+1 建议摘要。

---

## 我对当前 Trae 处理结果的评价

### 方向上：对

* `09` 负责后处理
* 新建净值曲线与交易报告脚本
* `11` 读净值并展示
  这个分层是对的。

### 还差的一步：把 `run_daily.py` 明确纳入

现在你给我的材料里，已经证明：

* 直接跑 `09_paper_trading_runner.py --mode live` 可以生成：

  * `live_equity_curve.csv`
  * `live_trade_report.csv`
* 飞书消息也能显示净值。

但**还没有直接证明 `run_daily.py` 已经把这条链完整串起来**。
所以这里不能默认“应该已经好了”，必须主动检查并补上。

---

## 你现在该怎么做

### 立刻做

1. 打开 `run_daily.py`
2. 确认它是否显式调用 `09_paper_trading_runner.py --mode live`
3. 确认 `09` 的执行发生在 `11_feishu_notification.py` 之前
4. 加入每日产物存在性检查：

   * 决策账本
   * 执行账本
   * 净值曲线
   * 交易报告
   * T+1 建议

### 不要做

5. 不要在 `run_daily.py` 里重复写净值和交易报告逻辑

---

## 最终明确结论

**这个问题的正确处理方式是：把净值曲线和交易报告正式纳入每日生产链，由 `09_paper_trading_runner.py` 统一生成，`run_daily.py` 负责调度和验收，`11_feishu_notification.py` 负责展示。**

**而且 `run_daily.py` 必须同步更新或至少同步校验这两个新产物，否则这次改动就还没有真正落到生产闭环。**
